require('dotenv').config();const fs=require('fs');const path=require('path');const express=require('express');const {Client,GatewayIntentBits,Partials,Collection}=require('discord.js');const {token,port}=require('./config');
if(!token)throw new Error('DISCORD_TOKEN is missing.');const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMembers,GatewayIntentBits.GuildMessages,GatewayIntentBits.MessageContent,GatewayIntentBits.GuildModeration,GatewayIntentBits.GuildVoiceStates],partials:[Partials.Channel,Partials.Message,Partials.User]});client.commands=new Collection();
function load(p){for(const f of fs.readdirSync(p)){const full=path.join(p,f);if(fs.statSync(full).isDirectory())load(full);else if(f.endsWith('.js')&&!f.startsWith('_')){const c=require(full);if(c.data?.name)client.commands.set(c.data.name,c);}}}load(path.join(__dirname,'commands'));const groups=require('./commands/groups');for(const c of Object.values(groups))if(c?.data?.name)client.commands.set(c.data.name,c);
client.on('interactionCreate',async i=>{if(!i.isChatInputCommand())return;const c=client.commands.get(i.commandName);if(!c)return;try{await c.execute(i)}catch(e){console.error(e);const msg={content:'❌ Something went wrong while executing that command.',ephemeral:true};if(i.replied||i.deferred)await i.followUp(msg).catch(()=>{});else await i.reply(msg).catch(()=>{});}});
require('./events/ready')(client);require('./events/messageCreate')(client);require('./events/prefixCommands')(client);require('./events/member')(client);require('./events/logs')(client);require('./events/antiNuke')(client);
const app=express();app.get('/',(_,r)=>r.send('Xieron HelpDesk is online.'));app.get('/health',(_,r)=>r.json({ok:true,uptime:process.uptime()}));app.listen(port,'0.0.0.0',()=>console.log(`Health server on ${port}`));client.login(token);
